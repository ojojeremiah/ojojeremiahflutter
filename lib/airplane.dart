class Planes{

  dynamic pictures;
  dynamic name;
  dynamic distance;
  dynamic speed;
  dynamic amount;
  dynamic about;

  Planes({required this.pictures, this.name, this.distance, this.speed, this.amount, this.about });
}
